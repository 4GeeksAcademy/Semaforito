
const Circulo = (props) => {

    return (
        
        <div onClick={props.onClick}>
            
            <div className={props.luz}>  </div>

        </div>

    );
}

export default Circulo;